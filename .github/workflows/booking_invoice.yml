name: Booking faktury – 10. den v měsíci

on:
  schedule:
    - cron: '0 8 10 * *'
  workflow_dispatch:

jobs:
  download-and-send:
    runs-on: self-hosted
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - name: Spust skript
        shell: cmd
        run: |
          C:\Users\jakes\AppData\Local\Programs\Python\Python312\python.exe -m pip install requests python-dateutil playwright --quiet
          C:\Users\jakes\AppData\Local\Programs\Python\Python312\python.exe booking_local.py
